//If I did anything other than this at work I'd be fired. Technically, this treats the numbers incorrectly, but blame Synxe.
String[] sortVersions(String[] versions) {
    Arrays.sort(versions);
    return versions;
}
